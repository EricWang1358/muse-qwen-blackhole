# GARGANTUA 建造实录：173 个回合，从白屏到精美

> 时间：2026-09-03 17:17（北京时间）起，至交付。
> 模型：队长 muse-spark-1.3；三名队员全程 muse-spark-1.3-contributor-free（免费档）。
> 方法：结对编程插件全程护航（两任小队），后半程队长亲自收尾。模型费用：¥0。
>
> **本文观点（先摆出来，再用下面 173 轮证明）：**
> **插件 + 时间 + 便宜模型 > 贵模型一轮出。**
> 一轮出的贵模型给不了你的三样东西——它证明不了自己没造假（本 session 实测：6 个全绿的静态验收与 78% 纯白画面共存），它不会在仪器撒谎时先毙掉自己的探针，它更没有判词必须重算的强制力。

## 0. 数据一览

| 项 | 数 |
|---|---|
| 总回合 | 173 |
| 人工提示词轮 | 17（编号 U1–U17，见 §2） |
| 结对小队 | 2 任（首任 6 卡 → gargantua-repair 11 卡） |
| 队长裁决 | 20+（链上可查 id） |
| P0/P1 风险单 | 10（全部以可执行证据关闭或诚实转卡） |
| 无头浏览器启动 | 100+ 次，实测截图 30+ 张 |
| 交付时控制台错误 | 0 |

## 1. 计数规则

- 1 回合 = 1 次 assistant 响应；U + 数字 = 人类消息轮。
- 子代理内部 turn 不单独计数，只记改变看板的事件。
- 同回合连续只读探测合并记一条；判词、风险单、冻结给完整编号。

## 2. 人工提示词 17 轮（全部）

| # | 内容 | 落点 |
|---|---|---|
| U1 | /pair 从零制作「GARGANTUA」（完整需求，吸积盘必须有体积） | R1 开队；6 张故事卡 |
| U2 | 继续 | 唤醒 Navigator 继续冻结 |
| U3 | 继续 | 成员 turn 失败后的恢复 |
| U4/U5 | 你可能需要新建小队（×2，去重确认） | 复盘 → 解散旧队 → 新队 |
| U6 | 刚才driver把chrome搞崩了，你继续 | 查进程、重启服务、浏览器军规 |
| U7 | 要不咱给子代理换一个模型继续跑？ | 论证不可换（spawn 快照绑定；重建将丢失可冻结性）→ 不换 |
| U8 | 能否减少验收轮次？20分钟轮次有变化吗 | 减轮 10–12→8；如实报告计数 0 但文件 +38KB |
| U9 | 搞定了吗？等什么后台任务 | 阻塞点通报；唯一后台任务是静态服务器 |
| U10/U11 | 还有多远（×2） | 距离核算（cycle 数 + 物理下限） |
| U12 | 结束了？ | 否：差冻结登记；随后 goal 到达解散小队 |
| U13 | webgl-libs 离线库安装结果粘贴 | 验明存在；裁决 vendor 锁死 0.160.0 |
| U14 | GitHub 仓库 URL | 建仓推送交付物 |
| U15 | 个人网页线上发布，关机可访问 | 开 GitHub Pages 并验活 |
| U16 | github 免费大约多久 | 搜索工具挂了如实说明；按官方口径回答 |
| U17 | 173 轮列单 + 标注人工轮 + 论证潜力与插件价值 + 宣传 | 即本文档 |

## 3. 回合流水（按阶段）

### P0 开队建卡（R1–R8）
R1 pair_start（full：Driver+Navigator+Challenger）+ 6 张故事卡。R2 令 Navigator 逐个 SPEC-FORK。R3 Challenger 规划攻击简报（三路线 + 6 陷阱）并入冻结条件。R4–R8 六个 oracle 逐个冻结（RED exit 1），21 参数名冻结。

### P1 脚手架（R9–R30）
index.html → main.js → vendor（verbatim three@0.160.0，需 GO）→ css → README → 冒烟。两次预期 partial REJECT → 仲裁沿判例回填 → gate 通过，t-1 完成。教训：工具禁 GO→VERIFIED 跳步，只能现树补 GREEN。

### P2 测地线核心（R31–R45）
P0 r-1：oracle 误伤 vendor（Sphere×39/TextureLoader×73）→ 重冻排除。单遍全屏积分 + CPU 镜像（临界碰撞翻转 + minR 3.42≈1.5rs）→ t-2 完成。

### P3 体积盘（R46–R58）
P0 r-2（反方向：vendor 独立满足 7/8，平盘 fake 可过，false-pass 更危险）→ 重冻。disk.js + 真体积接线 + edge-on PUFF 1.90/1.85 → t-3 完成。

### P4 星空后期（R59–R72）
P0 r-3（双重缺陷：vendor 纹理永红 + bloom/ACES 零行翻绿）→ 重冻。星空 → composer 窄放大 → 光度 probe（黑位 0/高光 2.4）→ 回填 → t-4 完成。

### P5 相机 HUD（R73–R95）
预防性加固重冻。params→uniforms→钩子→控制→HUD（keydown 缺一条惜败，补后翻绿）→ 交互证据 → 5 回填（预录仲裁）→ t-5 完成。

### P6 音频截图与白屏警报（R96–R112）
音频引擎 → 恢复浮层 → shot API。队长独立像素探针：78% 纯白，avgLum 222——6 grep 全绿而无黑洞。立 P0，CSS 反证 + HUD 佐证，三后端一致。

### P7 解散重建（R113–R122）
邮箱积压 52/66 连环失败 → 全套重跑取证（t-2/t-4 回归系 main.js:268 的 .png 字面量）→ retro → pair_stop（红 build 证据）→ 新队 + 4 卡。

### P8 仪器立法（R123–R150）
归档六标准 SHA256 三向等值；F1 单字面量定位；F4 零依赖探针；U6 崩溃后重启服务定军规；七连裁决（独立文件名/钉死单位/两档标准/反造假臂/上角母体/M5-M6/M7-M9）；Challenger 证伪 grep（删源 t-3 仍绿）、解码器同义反复、熵反诊断；Driver 自曝坏解码器（F5/F6/F7）。

### P9 定位与冻结（R151–R165）
NaN 假设被自己实验推翻；合成链被三帧真黑免罪；定位 diskCol 过曝。像素闸门冻结（M1 电池 + 变异体检出 + 双解码零分歧）。首个 photometry cycle 即遇 EXIT 2（M8 帧断言 flag 不对称）→ 一旗修正预授权。

### P10 用户密集介入（R166–R172）
换模型（论证不可换）/ 减轮次（10–12→8）/ 进度与距离核算 → goal 到达解散小队。

### P11 单人交付（R173 起至终验）
8077 被占→取证切 8091；雾里看洞→相机出盘→银河三收→盘发射→光子环收细→bloom 收紧→内缘收锋→回调甜点；两次误判当场更正（uGravAmt 行本就正确；preset 生效系肉眼误判）；标题/静音/tier/探针声明；rAF 确定性 bug 与时钟冻结修复；下载目录 4 次逐字节相同；ESM 全过；控制台零错误；移动端折叠面板；README/TESTLOG；根目录大扫除；交付截图。

### P12 发布（R173 之后）
验 webgl-libs → vendor 锁死裁决 → 建仓推送 ×2 → Pages 点亮验活（首页/JS 均 200）→ 免费问答 → 本文。

## 4. 证据链（哈希与数字，可重放）

- 归档封印 SHA256 三向等值；白帧 avgLum 222/whiteFrac 78% 三后端一致。
- 物理探针：minR 3.42、捕获逃逸翻转、PUFF 1.90/1.85、黑位 0/高光 2.4。
- 仪器功率：变异体 bpp 125269 / Paeth 31110 均被检出；双解码 worstDelta 0；电池 10/10。
- 确定性：同 URL 4 次逐字节相同（1712175 B）；渲染路径唯一随机源在音频缓冲。
- 交付判词：零控制台错误、7 模块 ESM 全过、21 参/4 预设/3 档/10 视图/快捷键/触摸/持久化/音频门禁/恢复处理器/移动端。

## 5. 为什么是插件 + 时间 + 便宜模型赢了

1. 贵模型一轮出最多给看起来对。本 session 实测：6 grep 全绿 + 78% 纯白同屏——一轮生成发现不了这个，因为它不验证自己。
2. 便宜模型够聪明，差的只是纪律。免费档干出：无浏览器 CPU 前向仿真复现 P0、三次自我撤回、解码器自曝。需要的是奖励上报的协议，不是更贵的权重。
3. 插件提供模型没有的东西：oracle-first、计算式判词、单写者、可执行关闭的风险单、重放门禁、back-pressure、三预算。每一项本 session 都真实开火。
4. 时间买的是仪器先于产品的复利：计数长期 0 的阶段是在造尺子；尺子好后修复只用了 86+12 行。
5. 总账：模型费 ¥0，交付带完整证据链、可重放、可审计的黑洞。

## 6. 为 dsh-pair-programming 说的话

仓库：https://github.com/EricWang1358/dsh-pair-programming —— GARGANTUA 是它的对抗性考场记录，不是 demo。它真实开火过的机制：SPEC-FORK、计算式 verify、gate 重放、风险单可执行证据、带预算仲裁、back-pressure、三预算、oracle_tampered 篡改检测。它真实暴露的缺陷（已全部 ticket 化，欢迎认领）：重封守卫只读 step（两次死锁两次搬卡）、verify/status 间歇非无损 JSON、CLOSED 终态无赋值路径、摘要失配直接抛错、red_exit 把 120 秒墙钟击杀记成品 RED。一个敢把自己缺陷也写进交付文档的插件，值得一个 star。

复现：git clone 本仓库 → 任意静态服务器 → 打开即看。TESTLOG.md 是验收记录。

Star it if you believe acceptance should be computed, not asserted.
